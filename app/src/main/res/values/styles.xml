<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="spinnerDropDownPlanStyle" parent="android:Widget.TextView.SpinnerItem">
        <item name="android:layout_height">30dp</item>
        <item name="android:background">@color/backgroundDropDownPlans</item>
        <item name="android:textColor">@color/fontDropDownPlans</item>
        <item name="android:textStyle">bold|italic</item>
        <item name="android:radius">10dp</item>
    </style>

    <style name="btnOptTaskStyle">
        <item name="android:layout_gravity">center_vertical|center_horizontal</item>
        <item name="android:layout_height">30dp</item>
        <item name="android:layout_weight">1</item>
    </style>

    <style name="dialogTaskStyle" parent="">
        <!--<item name="android:radius">10dp</item>-->
        <item name="android:background">@drawable/dialog_bg</item>
        <!--<item name="android:padding">10dp</item>-->
    </style>

</resources>
